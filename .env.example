# Values read by docker-compose.yml. Copy to .env before `docker compose up`.
# In dev you can skip this file entirely — the compose defaults are fine for local use.

JWT_SECRET=change-me-to-a-long-random-string-at-least-32-chars
